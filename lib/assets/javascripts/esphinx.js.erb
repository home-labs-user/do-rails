(function () {
    "use strict";

    var script;

    // src = tag
    //     .match(/(src=[\"\'])([\/\w])+(\.js)?/gi)[0]
    //         .replace(/(src=)|([\"\']*)/g, "");

    // script = document.querySelector("head script[src=\"" + src + "\"]");
    script = document.
        .querySelector('head script[src*="/esphinx/main"]');

    if (!script) {
        // window.onload = function () {};
        // script = document.createElement("script");
        // script.src = src;
        // document.head.appendChild(script);
        document.write(tag);
    }

})();
