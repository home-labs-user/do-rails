// require ./main

(function () {
    "use strict";

    var script;

    script = document.querySelector('head script[src*="/esphinx/main"]');

    if (!script) {
        // window.onload = function () {};
        // script = document.createElement("script");
        // script.src = src;
        // document.head.appendChild(script);
        document.write(script);
    }

})();
